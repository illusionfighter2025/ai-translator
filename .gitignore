# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Node
node_modules/
package-lock.json
yarn.lock

# Build artifacts (icons are committed; build script kept)
*.log

# Secrets — never commit real API keys
.env
.env.local
